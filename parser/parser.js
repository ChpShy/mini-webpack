
function tokenizer(input) {
    let current = 0;
    while(input[current]) {
        switch(input[current]) {
            case input[current] === '{':
                current++; 
        }

    }
}

function parser(input) {
    let token = tokenizer(input);


}

module.exports = {
    tokenizer,
    parser,
}